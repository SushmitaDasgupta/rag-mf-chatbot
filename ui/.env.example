# Local dev: leave empty to use Vite proxy (/api -> http://127.0.0.1:8000)
# Vercel production: set to Railway API URL without trailing slash
# See vercel.env.example at repo root and docs/deployment.md Phase 2.2
VITE_API_BASE_URL=
